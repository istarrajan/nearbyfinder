
  # Location-based service app

  This is a code bundle for Location-based service app. The original project is available at https://www.figma.com/design/hJFLbQS3dggf7ZId15gr4T/Location-based-service-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  